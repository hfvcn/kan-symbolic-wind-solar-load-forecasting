这是一份非常详尽的项目核查文档。我来为你系统分析后续应该怎么做：

---

## 后续行动分析

根据文档内容，你的项目目前处于**论文撰写前的证据整合阶段**。我将从三个维度给出建议：

---

### 一、论文撰写层面（优先级最高）

#### 1. 必须立即修正的表述

| 原有表述 | 应修正为 |
|---------|---------|
| "teacher-student 蒸馏" | "KAN teacher + post-hoc symbolic extraction" |
| "KAN 全程优于 PySR" | "当前 canonical matched 设置下 KAN 优于直接 PySR；历史探索期存在早期例外" |
| "严格无泄漏流程" | 删除或改为"近似因果流程，存在切分前插值边界" |

#### 2. 论文核心叙事线建议重构为：

```
问题发现 → 转向 focused teacher → solar 稳定正例 → wind 边界反例 → 可辨识性讨论
```

具体：
1. **开篇**：直接 net-load 路线下公式退化为负荷主导（用 `paperref_20260306_121725_v2__s1_delta_net_load_h6` 的 load-only 公式作为动机）
2. **方法转向**：引入 focused/component teacher 设计
3. **正例**：solar 物理量稳定进入公式（进入频率 3/3）
4. **反例与讨论**：wind 的低符号可辨识性 + horizon 非单调行为

#### 3. 论文定位调整

文档末尾已经明确指出，这篇论文更适合定位为：

> **"围绕变量可辨识性、替代表达与符号简洁性偏置的应用研究"**，而不是单纯的模型精度对比稿。

这意味着 Discussion 部分应该成为论文的重心，而不是 Results 中的精度表格。

---

### 二、实验补充层面（中等优先级）

文档指出了几个证据强度不足的地方，建议补充：

| 待补充项 | 目的 | 建议操作 |
|---------|------|---------|
| **多随机种子验证** | 提升命题 F 的可信度 | 至少跑 3-5 个不同 seed 的 canonical run |
| **solar long-horizon 补充** | 澄清 h288 的异常 (-7.628 skill) | 检查该 run 是否有训练异常或数据问题 |
| **matched PySR 公平性** | 回应审稿人可能质疑 | 考虑对齐 `max_train_rows` 和归一化策略后重跑 |

---

### 三、流程修复层面（可延后，但必须在论文中披露）

文档明确指出存在**非严格因果的插值泄漏**：

1. `interpolate_missing` 在切分前发生，使用了 `limit_direction="both"`
2. 气象对齐中的 `bfill()` 也在切分前

**建议处理方式**：

- **短期**（论文写作阶段）：在 Methods 或 Limitations 中如实披露，弱化机制性强表述
- **长期**（如有时间）：重构 pipeline 为切分后插值，重跑 canonical runs 验证结论是否稳定

---

### 四、具体执行清单

按优先级排序：

```
✅ 第一阶段（本周）
   □ 重写论文 Methods 部分，改用 "KAN teacher + symbolic extraction" 口径
   □ 整理 6 个命题的支撑证据表（直接引用本文档 Section 9）
   □ 撰写 Limitations 段落，披露插值泄漏边界

✅ 第二阶段（下周）
   □ 补充 3-5 个随机种子的 canonical run
   □ 检查 solar h288 异常 run
   □ Discussion 部分围绕"可辨识性 vs 预测价值"展开

✅ 第三阶段（投稿前）
   □ 考虑是否补充 matched PySR 的更公平实验
   □ 补充 unit tests（如果评审可能要求代码审查）
```

---

### 五、风险提示

1. **审稿人可能追问**：为什么早期 PySR > KAN，但你主张 KAN 更好？
   - 准备好"canonical vs 历史探索"的区分说明

2. **审稿人可能追问**：wind 进不去是不是 bug？
   - 准备好 Section 4 的流程检查证据（lag/scaler 无错位）

3. **审稿人可能追问**：为什么 solar h288 skill 是 -7.628？
   - 需要提前检查，否则会被质疑实验可靠性

---

需要我帮你进一步展开哪个部分的具体行动方案吗？比如论文结构草稿、实验补充脚本，或者 Limitations 段落的写法？