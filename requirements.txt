# KAN-SR Project Dependencies
# Data pipeline (Phase 1)
h5py>=3.10
s3fs>=2024.2.0
pandas>=2.0
numpy>=1.24
scikit-learn>=1.3
pvlib>=0.10.0
pyarrow>=14.0
tqdm>=4.66

# Modal cloud compute
modal>=0.60

# Phase 2+ (KAN training / symbolic / evaluation)
torch>=2.1
pykan==0.2.8
pyyaml>=6.0
sympy>=1.13.3
matplotlib>=3.8

# Development
ruff>=0.3.0
