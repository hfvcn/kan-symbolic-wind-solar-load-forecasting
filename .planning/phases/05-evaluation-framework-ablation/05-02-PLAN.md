---
phase: 05-evaluation-framework-ablation
plan: 02
subsystem: ablation
tags: [ablation, regularization, kan]

requires:
  - phase: 02-kan-architecture-sparse-training
provides:
  - "Regularization ablation runnable via CLI overrides"
  - "Pareto plot script for PySR vs KAN-SR"
---

# Phase 05 Plan 02: Regularization Ablation + Pareto Frontier

## Goal

为消融实验与帕累托分析提供“可跑”的代码接口与绘图工具：
- KAN 训练 CLI 支持覆盖 lamb 相关超参，便于跑 no_l1 / no_entropy / no_magnitude 等配置
- PySR equations.csv 增加每个 equation 的 test 指标列（test_rmse/test_mae/test_r2）
- 提供 PySR vs KAN-SR 的 Pareto frontier 绘图脚本

## Tasks

- [ ] 扩展 `modal_jobs/kan_train.py` local_entrypoint：支持 sparsify_lamb/l1/entropy/... 覆盖
- [ ] 扩展 `modal_jobs/pysr_baseline.py`：为每个 equation 计算并写出 test 指标
- [ ] 新增 `scripts/plot_pareto_frontier.py`

