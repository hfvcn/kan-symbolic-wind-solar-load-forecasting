---
phase: 03-symbolic-expression-extraction
plan: 01
subsystem: symbolic
tags: [sympy, auto_symbolic, latex, complexity, modal]

requires:
  - phase: 02-kan-architecture-sparse-training
    provides: "checkpoint/model.pt + feature_cols + target_scaler + cfg"
provides:
  - "Per-edge symbolic suggestion report (r2 + complexity)"
  - "Closed-form SymPy expression + LaTeX export"
  - "Formula evaluation metrics on test split"
---

# Phase 03 Plan 01: Symbolic Extraction (auto_symbolic + per-edge report)

## Goal

从 Phase 2 的稀疏 KAN 中提取可写入论文的闭式表达式：
- 逐边 suggest_symbolic，记录 best_name / r2 / complexity
- 选择性 fix_symbolic，并生成整体 symbolic_formula
- 产出 LaTeX 与复杂度指标（节点数/深度等）
- 在 test split 上评估公式（RMSE/MAE/R²）

## Tasks

- [ ] 新增 `src/kan_sr/symbolic.py`：边拟合、公式生成、复杂度与评估
- [ ] 新增 `modal_jobs/kan_symbolic.py`：读取 Phase 2 checkpoint + Phase 1 scaler，输出公式与报告
- [ ] 新增 `tests/test_symbolic_extraction.py`：合成数据端到端验证（线性函数可恢复）

