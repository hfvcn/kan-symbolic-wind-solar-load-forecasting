---
phase: 08-generalization-visualization
plan: 01
subsystem: transfer
tags: [generalization, iso, transfer, scaling, evaluation]

requires:
  - phase: 01-data-pipeline-infrastructure
  - phase: 02-kan-architecture-sparse-training
provides:
  - "Cross-ISO transfer evaluation (ERCOT -> other ISO) with consistent scaling"
---

# Phase 08 Plan 01: Cross-ISO Generalization (Transfer)

## Goal

完成 GENL-01：在至少一个其它 ISO（MISO / NYISO / SPP）上测试 ERCOT 训练的模型：
- Zero-shot：直接迁移（同一特征定义 + 同一训练 scaler）
- Few-shot（可选）：少量目标域数据微调或重新标定（论文可作为附加实验）

## Tasks

- [ ] 在 `src/data/split.py` 增加 “使用保存的 scaler_params 做正向标准化” 的函数（跨域统一尺度）
- [ ] 新增脚本 `scripts/transfer_eval.py`：读取 `runs/<train_run>` + `runs/<target_data_run>`，输出迁移评估 JSON/Parquet
- [ ] 将迁移结果纳入 Phase 5 对比表（generalization gap 指标）

