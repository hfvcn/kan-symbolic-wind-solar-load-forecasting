---
phase: 07-interpretability-validation
plan: 01
subsystem: sensitivity
tags: [interpretability, sensitivity, sympy, derivatives, eval]

requires:
  - phase: 03-symbolic-expression-extraction
provides:
  - "Sensitivity analysis artifacts (partial derivatives) and thesis-ready plots"
---

# Phase 07 Plan 01: Sensitivity Analysis (Partial Derivatives)

## Goal

对 Phase 3 提取的符号公式做偏导数敏感性分析（EVAL-07）：
- 基于 SymPy 计算 dY/dX（如 dLoad/dTemp、dPV/dGHI、dWind/dWindSpeed）
- 在测试集上统计导数分布（均值/中位数/符号比例）
- 生成论文级图表（直方图 / 箱线图 / QQ 诊断等）

## Tasks

- [ ] 为 `src/kan_sr/sensitivity.py` 增加单元测试（已知函数的导数应可恢复）
- [ ] 强化 `scripts/sensitivity_analysis.py`：输出标准化 CSV + 图表到 `doc/paper_assets/`
- [ ] 将偏导统计结果用于 Phase 7 物理解释映射（Plan 02 的输入）

