---
phase: 07-interpretability-validation
plan: 02
subsystem: physics_mapping
tags: [interpretability, physics, mapping, consistency, report]

requires:
  - phase: 03-symbolic-expression-extraction
  - phase: 07-interpretability-validation/07-01-PLAN.md
provides:
  - "Physics mapping report + physical consistency score for symbolic formulas"
---

# Phase 07 Plan 02: Physics Mapping & Validation

## Goal

将提取的公式映射到可解释的物理规律（EVAL-06），并形成可用于论文撰写的报告：
- 风电：识别 ~v³ 相关项（功率与风速立方）
- 光伏：识别 GHI 主导项 + 温度修正趋势（若存在）
- 负荷：识别温度敏感性（冷/热负荷）及符号合理性

同时输出一个“物理一致性评分”（用于 Phase 5 的多维评估表）。

## Tasks

- [ ] 新增 `src/eval/physics_mapping.py`（基于 SymPy 的模式匹配与统计）
- [ ] 新增 `scripts/physics_mapping.py`：读取 `runs/<symbolic_run>` 生成 `doc/paper_assets/physics_mapping_<run>.md`
- [ ] 在报告中引用 Plan 01 偏导统计（符号比例、极值、是否出现异常爆炸）

