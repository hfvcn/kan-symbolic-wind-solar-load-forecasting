---
phase: 01-data-pipeline-infrastructure
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/__init__.py
  - src/config.py
  - src/data/__init__.py
  - src/data/download.py
autonomous: true
requirements:
  - DATA-01

must_haves:
  truths:
    - "ARPA-E PERFORM ERCOT HDF5 data downloads from S3 without credentials"
    - "Downloaded HDF5 files are cached locally on Modal Volume"
    - "Re-running download skips already-cached files (idempotent)"
    - "Wind, solar, and load actuals load into pandas DataFrames with UTC timestamps"
  artifacts:
    - path: "src/__init__.py"
      provides: "Package marker for src module"
      min_lines: 1
    - path: "src/config.py"
      provides: "Centralized configuration (paths, ERCOT location, parameters)"
      contains: "ERCOT_LATITUDE"
    - path: "src/data/__init__.py"
      provides: "Package marker for data submodule"
      min_lines: 1
    - path: "src/data/download.py"
      provides: "S3 HDF5 download with caching logic"
      exports: ["download_perform_data", "load_hdf5_to_dataframe"]
  key_links:
    - from: "src/data/download.py"
      to: "s3://arpa-e-perform/"
      via: "s3fs anonymous access"
      pattern: "S3FileSystem\\(anon=True\\)"
    - from: "src/data/download.py"
      to: "src/config.py"
      via: "import for paths and parameters"
      pattern: "from src.config import"
---

<objective>
Create the foundational data acquisition module for downloading ARPA-E PERFORM ERCOT data from AWS S3.

Purpose: Establish project structure and implement idempotent HDF5 download with caching, enabling all subsequent preprocessing and feature engineering.

Output: src/ module structure with config.py and data/download.py that can fetch wind, solar, and load data from PERFORM S3 bucket.
</objective>

<execution_context>
@/Users/vfch/.claude/get-shit-done/workflows/execute-plan.md
@/Users/vfch/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/MODAL.md
@.planning/phases/01-data-pipeline-infrastructure/01-RESEARCH.md
@modal_jobs/smoke_test.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create project structure and configuration</name>
  <files>src/__init__.py, src/config.py, src/data/__init__.py</files>
  <action>
Create the src/ module structure following the research-recommended layout:

1. Create `src/__init__.py` with minimal docstring identifying the package.

2. Create `src/config.py` with:
   - `VOLUME_MOUNT = "/vol"` (Modal Volume mount point)
   - `ERCOT_LATITUDE = 31.0` and `ERCOT_LONGITUDE = -100.0` (system centroid for solar calculations)
   - `S3_BUCKET = "arpa-e-perform"` and S3 path templates for wind/solar/load actuals
   - `DEFAULT_YEAR = 2018` (primary data year)
   - `RESOLUTION_MINUTES = 5` (5-minute data resolution)
   - `LAG_WINDOW = 48` (t-1 to t-48 = 4 hours at 5-min)
   - `TRAIN_RATIO = 0.7`, `VAL_RATIO = 0.15` (test = 0.15)
   - Path helper functions: `get_run_dir(run_id)`, `get_raw_dir(run_id)`, `get_processed_dir(run_id)`

3. Create `src/data/__init__.py` with docstring and `__all__` listing public modules.

Use Python 3.11+ type hints throughout. Follow existing project conventions from smoke_test.py (UTC timestamps, snake_case).
  </action>
  <verify>
    <automated>python -c "from src.config import ERCOT_LATITUDE, S3_BUCKET, get_run_dir; print('config imports ok')"</automated>
  </verify>
  <done>src/ module imports successfully; config.py contains all required constants and path helpers</done>
</task>

<task type="auto">
  <name>Task 2: Implement HDF5 download with S3 anonymous access</name>
  <files>src/data/download.py</files>
  <action>
Create `src/data/download.py` implementing idempotent ARPA-E PERFORM data download:

1. `download_perform_file(s3_path: str, local_path: str, force: bool = False) -> str`:
   - Check if local_path exists; if so and not force, log "skipping, cached" and return path
   - Use `s3fs.S3FileSystem(anon=True)` for credential-free access
   - Stream HDF5 from S3 to local_path with progress logging (use tqdm if available, else simple prints)
   - Return the local path
   - Handle S3 errors gracefully with clear error messages

2. `download_ercot_actuals(run_dir: str, year: int = 2018, data_types: list[str] = None) -> dict[str, str]`:
   - Default data_types = ["wind", "solar", "load"]
   - For each type, construct S3 path using config templates
   - Call download_perform_file to cache in `{run_dir}/raw/`
   - Return dict mapping type -> local file path

3. `load_hdf5_to_dataframe(file_path: str, dataset_key: str = "actuals") -> pd.DataFrame`:
   - Open HDF5 with h5py
   - Read time_index (convert bytes to datetime), meta (for column names), and actuals
   - Build DataFrame with UTC-localized DatetimeIndex
   - Apply scale_factor from attributes if present (log if missing)
   - Return DataFrame with proper column names from meta

4. `load_all_actuals(run_dir: str, year: int = 2018) -> dict[str, pd.DataFrame]`:
   - Convenience function that downloads (if needed) and loads all three data types
   - Returns dict mapping "wind"/"solar"/"load" -> DataFrame

Include detailed docstrings explaining HDF5 structure assumptions based on PERFORM documentation.
Log all operations for thesis reproducibility (per user decision).
  </action>
  <verify>
    <automated>python -c "from src.data.download import download_perform_file, load_hdf5_to_dataframe; print('download module ok')"</automated>
    <manual>Run `modal run` test to verify S3 access works (requires Modal environment)</manual>
  </verify>
  <done>download.py exports all functions; module imports without errors; docstrings explain HDF5 structure</done>
</task>

</tasks>

<verification>
1. `python -c "from src import config, data"` succeeds
2. `python -c "from src.data.download import load_all_actuals"` succeeds
3. All config constants are accessible and correctly typed
4. Download functions have proper type hints and docstrings
</verification>

<success_criteria>
- src/ module structure exists with proper __init__.py files
- config.py contains all required constants (ERCOT location, S3 paths, split ratios, lag window)
- download.py implements idempotent S3 download with caching
- HDF5 loading handles PERFORM data structure (time_index, meta, actuals)
- All code uses Python 3.11+ type hints
- Detailed logging for thesis documentation
</success_criteria>

<output>
After completion, create `.planning/phases/01-data-pipeline-infrastructure/01-01-SUMMARY.md`
</output>
