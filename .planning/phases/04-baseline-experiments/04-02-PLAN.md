---
phase: 04-baseline-experiments
plan: 02
subsystem: torch-baselines
tags: [pytorch, mlp, lstm, baseline, modal]

requires:
  - phase: 01-data-pipeline-infrastructure
    provides: "processed Parquet splits"
provides:
  - "MLP baseline with metrics + checkpoint"
  - "LSTM baseline with metrics + checkpoint"
---

# Phase 04 Plan 02: LSTM/MLP Baselines + Metrics

## Goal

实现两个深度学习基线：
- MLP：静态特征回归
- LSTM：序列建模（使用非 lag 特征构建窗口序列）

要求：
- 使用 Phase 1 相同的 train/val/test split（chronological）
- 输出 test set 的 RMSE/MAE/R²
- 可选：对齐 KAN 参数量（通过 Phase 2 checkpoint 的 state_dict 估算）

## Tasks

- [ ] 新增 `src/baselines/torch_models.py`（MLP/LSTM）
- [ ] 新增 `src/baselines/torch_training.py`（MLP 训练器 + LSTM 序列构建）
- [ ] 新增 `modal_jobs/baseline_torch.py`（Modal 作业：mlp/lstm 两种模式）

