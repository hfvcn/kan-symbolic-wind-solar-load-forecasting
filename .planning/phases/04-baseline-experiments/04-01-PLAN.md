---
phase: 04-baseline-experiments
plan: 01
subsystem: pysr
tags: [pysr, julia, symbolic-regression, baseline, modal]

requires:
  - phase: 01-data-pipeline-infrastructure
    provides: "processed Parquet splits"
provides:
  - "PySR baseline job producing equations Pareto frontier + test metrics"
---

# Phase 04 Plan 01: PySR Baseline (isolated job)

## Goal

在独立环境中运行 PySR（Julia 后端），产生：
- `equations.csv`（Pareto/frontier）
- best equation 文本（用于论文）
- test set 的 RMSE/MAE/R²

## Tasks

- [ ] 新增 `modal_jobs/pysr_baseline.py`
- [ ] 读取 Phase 1 Parquet，使用与 KAN 相同的特征选择（默认 lag 子集）
- [ ] 保存方程表格与指标到 artifacts

